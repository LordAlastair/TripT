version: '2'
services:
  webservice:
    build:
      context: ./server
      dockerfile: Dockerfile
    command: sh -c 'npm install; npm run migrate; npm run seed; npm start'
    # command: npm start
    volumes:
      - ./server:/server
    depends_on:
      - db
    ports:
      - 3000:3000
    links:
      - db
    environment:
      - NODE_ENV=development

  db:
    image: postgres:9.4
    ports:
      - 32776:5432
    environment:
      - POSTGRES_PASSWORD=passw0rd
      - POSTGRES_USER=admin
      - POSTGRES_DB=vagalivre
      - PGDATA=/var/lib/dev-db/data
